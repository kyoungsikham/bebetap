import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test', (WidgetTester tester) async {
    // Phase 1 스캐폴딩 — 실제 테스트는 각 Phase에서 추가
    expect(true, isTrue);
  });
}
